include_rules

OBJS = $(SRC)/*.o

CPPS = $(SRC)/*.cpp

CPPS += $(BOOST_CONTEXT)/asm/ontop_x86_64_sysv_elf_gas.S
CPPS += $(BOOST_CONTEXT)/asm/make_x86_64_sysv_elf_gas.S
CPPS += $(BOOST_CONTEXT)/asm/jump_x86_64_sysv_elf_gas.S
CPPS += $(BOOST_CONTEXT)/posix/stack_traits.cpp
CPPS += $(BOOST_CONTEXT)/execution_context.cpp

: foreach $(CPPS) |> !cx |> %B.o
: $(OBJS) |> ar crs %o %f |> ../$(LIBRARY_NAME) ../<cosche>
